<?php
// CRUD do usuario

class Usuario
{
    public function verificarLogin($email, $senha)
    {
        //usar data::load(usuario) e verificar se login e senha existe
    }

    public function save($usuario, $senha)
    {
        // usar data::load(usuario) verificar se existe e usar data::save(usuario)
        // $data = new data;
        // $data-> save(usuario);
    }
}